name: Streamlit Keep Alive
on:
 schedule:
   # Her saat başı çalışır
   - cron: '0 * * * *'
 workflow_dispatch:
jobs:
 ping:
   runs-on: ubuntu-latest
   steps:
     - name: Send ping request to Streamlit app
       run: |
         curl -s https://ofis-rezervasyon-f6taul96egtiydkwg2jbas.streamlit.app > /dev/null
